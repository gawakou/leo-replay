[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leo-replay"
version = "0.5.0"
description = "Measurement-driven replay framework for LEO communication behavior"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["skyfield>=1.54,<2"]

[project.scripts]
leo-replay = "leo_replay.cli:main"
leo-replay-measure = "leo_replay.measure_cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"

[tool.ruff]
target-version = "py310"
line-length = 100
[tool.setuptools.package-data]
"leo_replay.visualization" = ["assets/*"]
