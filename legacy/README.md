# Legacy scripts

These files are preserved from the router snapshot for provenance only.
They include old variants, hard-coded local settings, and sibling-path assumptions.
Do not use them as current entry points. Active scripts are under `scripts/`.
