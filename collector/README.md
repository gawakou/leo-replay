# Collector placeholder

The uploaded router archive did not contain the Starlink measurement collector.
When it is shared, add it here with:

- a stable raw-data schema
- time-zone and clock synchronization rules
- ping/iperf3/gRPC execution settings
- a synthetic fixture for tests
- no credentials or terminal-specific identifiers
